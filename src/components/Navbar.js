import { useSelector } from "react-redux";

const Navbar = () => {
    return (
        <nav>
            <div className='nav-center'>
                <h3>Redux toolkit</h3>
            </div>
        </nav>
    )
}